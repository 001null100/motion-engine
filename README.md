# Motion Engine

A bespoke Windows VST3 modulation experiment built around stateful 2D motion and physics.

This repository currently contains the **Bitwig integration/performance spike**. Its purpose is to measure how well a companion Bitwig Controller Extension can drive arbitrary Bitwig/device parameters from Motion Engine before the full plugin is built.

See `docs/TESTING.md` for the benchmark procedure.
